// App.tsx

// Importa as rotas da aplicação
import AppRoutes from './routes';

// Importa os estilos globais (Tailwind, fontes, etc.)
import './index.css';

// Componente principal da aplicação
function App() {
  // Aqui apenas retornamos as rotas. Toda a lógica está separada.
  return <AppRoutes />;
}

export default App;
